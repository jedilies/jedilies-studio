# Structure
```
root:
  projects (all items with tagged projects)
    projectTitle
      all docs per projectTitle
  settings (all docs tagged settings)
    tags
    categories
  people (all docs tagged people)
  misc (all doc types without a tag)
```